# Codex acts as the host/judgment path in this pilot, so keep a full-capability default.
CLAUDE_DEFAULT_MODEL=claude-sonnet-4-6
GEMINI_DEFAULT_MODEL=gemini-3.1-pro-preview
CODEX_DEFAULT_MODEL=gpt-5.4
